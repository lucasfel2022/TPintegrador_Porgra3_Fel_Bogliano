════════════════════════════════════════════════════════════════════════
CAMBIOS A APLICAR EN Back/src/models/index.js (tu archivo real)
════════════════════════════════════════════════════════════════════════

⚠️ OJO: no vi tu index.js real. Esto asume el patrón MÁS COMÚN en
proyectos de la materia (require directo de cada modelo + export).
Si tu index.js usa fs.readdirSync() para auto-cargar modelos, NO
necesitás tocar nada — Encuesta.js ya va a entrar solo porque está
en la carpeta models/. Mandame tu index.js real para confirmar cuál
de los dos casos es y ajusto si hace falta.

PATRÓN A (require manual — el más probable dado que vi requires
explícitos como { Venta, Producto, VentaProducto, LogSesion } en
registrosController.js):

  const Usuario       = require('./Usuario');
  const Producto      = require('./Producto');
  const Venta         = require('./Venta');
  const VentaProducto = require('./VentaProducto');
  const LogSesion     = require('./LogSesion');
  const Encuesta      = require('./Encuesta');   // ← AGREGAR

  // ... asociaciones existentes (Venta.belongsToMany(Producto, ...), etc.)

  // Si querés asociar Encuesta a Venta (campo venta_id ya está en el modelo):
  Venta.hasOne(Encuesta, { foreignKey: 'venta_id' });
  Encuesta.belongsTo(Venta, { foreignKey: 'venta_id' });

  module.exports = {
    Usuario,
    Producto,
    Venta,
    VentaProducto,
    LogSesion,
    Encuesta,   // ← AGREGAR
  };

PATRÓN B (auto-carga con fs.readdirSync):
  No hace falta tocar nada, Encuesta.js se carga sola.
